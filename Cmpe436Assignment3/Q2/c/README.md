# Eventually Entering Critical Section
With the LTL unary operator <>, we can check whether a predicate holds eventually. That is what we need at this point.

Mentioned LTL rule is inlined into source program.
